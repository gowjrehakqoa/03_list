#include <stdio.h>
#include <queue>

int main(int argc, char *argv[]) 
{
    queue<int> q; 
    char str[5];
    int num , count =0;
    while(fgets(str, sizeof(str), stdin)){
      q.push(1);
      q.push(2);
      q.push(3);
      q.push(4);
       q.push(5);
        if(q.push == 0){
            printf("r,\n");
        }
        else if(q.push == -1){
            
            
    }

    return 0;
}
